let notas = [3,5,8,10,9,7,8,8];

let posicionDelOcho = notas.lastIndexOf(8);  // Para saber la ultima posiciòn de un parametro repetido.

console.log(posicionDelOcho);


let notas1 = [3,5,8,10,9,7,8,8];

let resultadoJuntado = notas1.join(",* ");  //Join para juntar todos los parametro de la variable del array.

console.log(resultadoJuntado);

// Join = Para extraer
// Push = para insertar elementos al final.
// Shift = Para extraer 
// Unshift = Para insertar elemento al principio de un array.
// IndexOf  ||  LastIndexOf = para preguntar el indice de una ocurrencia. 
// Join = para unificar todos los elementos presentes en una array.
// Includes // Retorna un booleano osea true o false. 

